package seedu.address.logic.commands;

/**
 * Contains integration tests (interaction with the Model, UndoCommand and RedoCommand) and unit tests for EditCommand.
 */
public class EditCommandTest {
    /*
    TODO:
    1. works when all fields specified
    2. works when some optional fields unspecified
    3. works when no fields specified?
    4. works on a filtered list (filtered w find command)
    5. fails when edit results in a duplicated item in the list
    6. fails when index is out of bounds
     */

}
