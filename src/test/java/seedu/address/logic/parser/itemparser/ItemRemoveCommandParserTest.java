package seedu.address.logic.parser.itemparser;

import org.junit.jupiter.api.Test;

public class ItemRemoveCommandParserTest {

    //    private RemoveCommandParser parser = new RemoveCommandParser();

    @Test
    public void parse_allFieldsPresent_success() {
    //    Index targetIndex = INDEX_FIRST_ITEM;
    //
    //    String userInput = targetIndex.getOneBased() + QUANTITY_DESC_DUCK;
    //
    //    Item item = new ItemBuilder().withQuantity(VALID_QUANTITY_DUCK)
    //            .build();
    //    RemoveCommand expectedCommand = new RemoveCommand(targetIndex, item.getQuantity());
    //
    //    assertParseSuccess(parser, userInput, expectedCommand);
    }
}
