package seedu.address.model.meeting;

import java.util.Comparator;

/**
 * Abstract class which is used to compare meetings based on meeting date.
 */
public abstract class MeetingComparator implements Comparator<Meeting> {
}
