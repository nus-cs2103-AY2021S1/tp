package seedu.address.logic.parser;

import seedu.address.logic.commands.ViewCalendarCommand;
import seedu.address.logic.parser.exceptions.ParseException;

/**
 * Parses input arguments and creates a new ViewCalendarCommand object
 */
public class ViewCalendarParser implements Parser<ViewCalendarCommand> {
    /**
     * Parses the given user input and returns a ViewCalendarCommand
     * for execution.
     * @param userInput user arguments.
     * @return ViewCalendarCommand to be executed.
     * @throws ParseException if the user input does not conform to the expected format.
     */
    @Override
    public ViewCalendarCommand parse(String userInput) throws ParseException {
        return null;
    }
}
